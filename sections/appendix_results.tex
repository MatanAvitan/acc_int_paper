% Appendix snapshot tables
\section{Single-Model Layer-8 Comparison (All Methods)}
\label{sec:appendix_full_comparison}

Table~\ref{tab:full_comparison} reports the full eight-method comparison at layer~8 of Qwen2.5-0.5B across sycophancy, gender, and safety. This table provides the detailed single-model benchmark that supports the multi-model summary in the main text.

\input{tables/full_comparison}

\section{All Methods, All Models, All Datasets}
\label{sec:appendix_all_methods}

Tables~\ref{tab:appendix_all_methods_sycophancy}, \ref{tab:appendix_all_methods_gender}, and~\ref{tab:appendix_all_methods_safety} report nonlinear leakage (NL) and control-task change ($\Delta$Ctrl) for all eight erasure methods across thirteen models spanning five families (Qwen2.5, Gemma-2, Llama-3.2, Mistral, Gemma-3) and all three concept domains. Values are mean\,$\pm$\,std over the 25\%, 50\%, and 75\% depth layers. The lowest NL per model among non-random methods is marked in \textbf{bold}. Gemma-3 coverage now includes completed 1B, 4B, 12B, and 27B results in the aggregate tables; the per-layer table below is narrower and should be read as the supporting breakdown for the models currently mirrored there.

\input{tables/appendix_all_methods}

\section{Legacy multi-model comparison (global-$\lambda$ protocol)}
\label{sec:appendix_legacy_multimodel}

Tab.~\ref{tab:multi_model} reports the eight-method comparison across 13 model families and 3 concepts under the legacy global-$\lambda$ HPO protocol: each $\lambda$-based method selects a single $\lambda^* \in \{0.25, 0.5, 1, 2, 4, 8, 16, 32\}$ per panel by lowest validation nonlinear-probe accuracy. We retain this as the only comparison axis covering Random Global, Random Per-Sample, Linear Projection, INLP, IGBP, and GCE-Ambient at scale; the headline pipeline does not use this grid. Tab.~\ref{tab:multi_model_per_layer} expands the same comparison at 25\%, 50\%, and 75\% depth.

\input{tables/multi_model_comparison}

\input{tables/multi_model_per_layer}

\section{Global-$\lambda$ lambda sweep ablation}
\label{sec:appendix_lambda_sweep}

Tab.~\ref{tab:lambda_sweep} isolates the effect of step size on the legacy global-$\lambda$ form of \AmbGCE{} and \TanGCE{} at three fixed values ($0.5, 4.0, 8.0$) on Qwen2.5-0.5B layer~8. As $\lambda$ increases, \AmbGCE{} leakage worsens (rising to $0.734/0.702/0.770$ on syc/gen/saf at $\lambda{=}8$); \TanGCE{} improves and reaches its best precision at the largest tested step size. We retain this as a legacy ablation; the proposed pipeline replaces the global $\lambda$ with the closed-form per-row $\lambda_i$ of Eq.~\ref{eq:lambda_closed_form}.

\input{tables/lambda_sweep}

\section{Phase-1 closed-form ablation}
\label{sec:appendix_phase1_ablation}

Tab.~\ref{tab:leace_vs_tangce_nlp} reports a fresh nonlinear probe ($h{=}128$, 200 SGD steps, patience 3) trained on three representation states per panel: clean, post-LEACE, post-(LEACE\,+\,CovMatch K=2). Sycophancy is fully exhausted by Phase~1 (mean NL $0.563$ indistinguishable from floor $0.560$ and from full \TanGCE{}'s $0.560$). Gender is not: Phase~1 leaves $14.8$pp residual above floor that the iterative Phase~2 closes to $1.0$pp. Safety follows the gender pattern with $11.5 \to 0.2$pp.

\input{tables/leace_vs_tangce_nlp}

\section{Per-model bar charts (sycophancy and safety)}
\label{sec:appendix_per_model_bars}

Sycophancy and safety analogues of the gender per-model bar chart in the main text (Fig.~\ref{fig:per_model_bars_gender}). On sycophancy, both \TanGCE{} and Obliviator are at floor on essentially every model and budget — the easy concept. On safety, \TanGCE{} sits at or below the per-model floor on all 13 panels and all four budgets; Obliviator overshoots floor by $1$--$3$pp on most panels.

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{figures/per_model_bars_sycophancy.png}
\caption{\textbf{Sycophancy: per-model nonlinear-probe accuracy at four control-damage budgets.} Layout matches Fig.~\ref{fig:per_model_bars_gender}.}
\label{fig:per_model_bars_sycophancy_app}
\end{figure}

\begin{figure}[h]
\centering
\includegraphics[width=\linewidth]{figures/per_model_bars_safety.png}
\caption{\textbf{Safety: per-model nonlinear-probe accuracy at four control-damage budgets.} Layout matches Fig.~\ref{fig:per_model_bars_gender}.}
\label{fig:per_model_bars_safety_app}
\end{figure}

\section{NLP full per-panel breakdown}
\label{sec:appendix_per_panel}

Table~\ref{tab:per_panel_breakdown} reports the per-panel version of the main-text Tab.~\ref{tab:main_results_unified}: for each of the 39 NLP panels and each of the four control-damage budgets, the signed deviation $(S - S_{\text{floor}})$ in pp at the trajectory step closest to floor for all five methods. Bold marks the row-best per panel per budget block (every method within numeric precision of the row-best is bolded). The trailing rows summarize across panels: per-method mean and \TanGCE{} W/T/L count vs.\ the best other method, with the $0.5$pp tie band ensuring that any panel where the row-best is tied between \TanGCE{} and another method counts as T (not W).

\input{tables/per_panel_breakdown}

\section{CelebA full per-attribute breakdown}
\label{sec:appendix_celeba_extended}

Table~\ref{tab:celeba_per_panel_breakdown} reports the per-attribute version of the main-text Tab.~\ref{tab:celeba_unified_summary}: for each of the 40 CelebA-CLIP attributes and each of the two control regimes, the signed deviation $(S - S_{\text{floor}})$ in pp at the trajectory step closest to the per-attribute majority-class floor, subject to mean $\Delta Y \ge -5$pp on the 5 controls. Bold marks the row-best per regime block. The trailing rows summarize across the 40 attributes: per-method mean and \TanGCE{} W/T/L count vs.\ the best other method (0.5pp tie band).

\input{tables/celeba_per_panel_breakdown}

Fig.~\ref{fig:per_attr_bars_celeba} reports the same comparison restricted to \TanGCE{} vs.\ Obliviator as a per-attribute floor-distance win-margin plot, separately for the 5-least- and 5-most-correlated control regimes. Hatched dark-blue bars mark walkover panels where \TanGCE{} has at least one in-budget step but Obliviator has none; these are exactly the entangled-target attributes that Tab.~\ref{tab:celeba_unified_summary} flags as walkovers in the most-correlated regime.

\begin{figure}[h]
\centering
\begin{minipage}{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{figures/pareto_celeba_sota_least_winmargin_multi_floor.png}
\subcaption{5 least-correlated controls (low entanglement).}
\end{minipage}\hfill
\begin{minipage}{0.49\linewidth}
\centering
\includegraphics[width=\linewidth]{figures/pareto_celeba_sota_most_winmargin_multi_floor.png}
\subcaption{5 most-correlated controls (high entanglement).}
\end{minipage}
\caption{\textbf{CelebA per-attribute floor-distance win-margin.} Solid blue/red = $\bigl(|S^{\text{Obliv}} - S_{\text{floor}}| - |S^{\TanGCE{}} - S_{\text{floor}}|\bigr)\!\times\!100$ on panels where both methods reach the budget; positive (blue) = \TanGCE{} closer to floor. Hatched dark-blue ($^{\star}$) marks walkover panels where \TanGCE{} has at least one in-budget step but Obliviator has none. Tie band $\pm 0.5$pp shaded.}
\label{fig:per_attr_bars_celeba}
\end{figure}

\section{Supplementary Deeper-Layer Comparisons}
\label{sec:appendix_snapshots}

This appendix records broader deeper-layer comparisons that are directionally consistent with the main text but are not part of the core quantitative evidence used for the paper's principal claims. We include them to show how the same comparison pattern extends beyond the main layer-8 evaluation and to motivate future matched-leakage analyses.

\subsection{Iterative Sycophancy (Supplementary Comparison)}
\input{tables/repo_snapshot_sycophancy}

\subsection{Iterative Safety (Supplementary Comparison)}
\input{tables/repo_snapshot_safety}

\subsection{Local CelebA Visual Table-2-Style Comparison}
\input{tables/local_celeba_visual_table2}

To provide a modality-matched supplementary slice, this appendix reports a local vision-only CelebA run (CLIP ViT-B/32 embeddings) over the same method family, with three least-correlated controls per target concept. This local check is separate from the paper-locked main-text protocol and is included only as supplementary context.

\input{tables/main_text_geometry_schedule}

\paragraph{Geometry Note.}
The broader geometry report continues to support three useful points: early and mid-layer representations are effectively low-dimensional, on-manifold directions carry much of the model's sensitivity in these perturbation tests, and interpolation remains consistent with a connected representation space. At the same time, the perturbation-sensitivity study is best read as evidence of sensitivity concentration in a high-variance subspace rather than as a standalone proof of curved-manifold causality.

\section{Intuition: ``fraction of the local whitened neighborhood''}
\label{sec:appendix_fmaha_intuition}

Section~\ref{sec:closed_form_lambda} introduces the per-sample trust region $\| \tilde{\mathbf{x}}_i - \mathbf{x}_i \|_2 \leq \varepsilon \cdot s_i$ and offers two choices for the local scale $s_i$: the simple radial $k$-NN distance $r_i$, or the anisotropy-aware whitened-tangent radius (Eq.~\ref{eq:s_fmaha}). The paper says $\varepsilon$ then becomes ``a fraction of the local whitened-tangent neighborhood.'' This appendix unpacks what that phrase means in concrete terms.

\paragraph{Reading the trust region as a budget.}
With $s_i = r_i$ (the radial choice), the bound reads ``move at most $\varepsilon \cdot$ Euclidean $k$-NN radius.'' This is direction-agnostic: every direction inside ambient space gets the same budget. With $s_i$ defined by Eq.~\ref{eq:s_fmaha} (the FMaHa whitened-tangent radius), the bound instead reads ``move at most $\varepsilon$ of the neighborhood radius \emph{after normalising each local tangent axis by its own scale}.'' That distinction is what makes $\varepsilon$ direction-aware.

\paragraph{Worked example.}
Suppose the local tangent space at $\mathbf{x}_i$ is two-dimensional with right singular vectors $\mathbf{v}_1, \mathbf{v}_2$ and singular values $\sigma_{i,1} = 10$, $\sigma_{i,2} = 1$. Neighbors of $\mathbf{x}_i$ extend ten times further along $\mathbf{v}_1$ than along $\mathbf{v}_2$:
\[
\text{$\mathbf{v}_1$ direction: long / fat / well-supported,} \qquad
\text{$\mathbf{v}_2$ direction: short / thin / fragile.}
\]
The whitened tangent coordinate of a neighbor offset $\mathbf{x}_j - \mathbf{x}_i$ along $\mathbf{v}_k$ is
$z_k = \langle \mathbf{x}_j - \mathbf{x}_i, \mathbf{v}_k \rangle / \sigma_{i,k}$. Moving 1 ambient unit along $\mathbf{v}_1$ is therefore $1/10 = 0.1$ \emph{whitened} units, while moving 1 ambient unit along $\mathbf{v}_2$ is $1/1 = 1$ whitened unit. Expressed in whitened coordinates the local neighborhood is approximately spherical: ``10\% of the whitened neighborhood'' is the same allowed move \emph{regardless of direction inside the tangent}. Translated back through the singular spectrum, that allowed move is large in ambient norm along $\mathbf{v}_1$ and small along $\mathbf{v}_2$ — exactly the asymmetry we want, because the linear approximation is reliable along high-$\sigma$ axes and fragile along low-$\sigma$ ones.

\paragraph{Comparison with the Euclidean radius.}
The Euclidean form is the inequality
\[
\| \boldsymbol{\delta} \|_2 \;\leq\; \varepsilon \cdot r_i,
\]
which treats every direction equally. The whitened form is approximately
\[
\Big\| \mathbf{B}_i^\top \boldsymbol{\delta} \,/\, \boldsymbol\sigma_i \Big\|_2 \;\leq\; \varepsilon \cdot R_i^{\mathrm{white}},
\]
where division is component-wise and $R_i^{\mathrm{white}}$ is the median neighbour displacement in standardised tangent coordinates (Eq.~\ref{eq:s_fmaha}). Reading this inequality in plain English: \emph{after dividing each tangent coordinate of the edit by its local standard scale $\sigma_{i,k}$, the edit must be at most $\varepsilon$ times the typical neighbour displacement on that scale}. The same edit, projected onto a high-$\sigma$ axis, is allowed a larger ambient stride; projected onto a low-$\sigma$ axis, it is held to a much smaller stride.

\paragraph{Mental model.}
Imagine the local neighborhood as an ellipse stretched along its high-$\sigma$ axis and pinched along its low-$\sigma$ axis. A ``$\varepsilon$ Euclidean'' step ignores the ellipse shape — it steps the same length in any direction. A ``$\varepsilon$ whitened-neighborhood'' step instead asks: \emph{after reshaping the ellipse into a sphere, does the edit fit inside an $\varepsilon$ ball?} FMaHa then maps the allowed move back into the original representation space. The result is a direction-aware trust region: it prevents the iterative loop from taking a step that is numerically small in Euclidean norm but huge relative to a thin local tangent direction where the linear approximation is the least reliable.

This is the geometric reason why $\varepsilon = 0.1$ is interpretable as an order-of-magnitude smaller knob than the legacy global $\lambda$ used in the appendix tables: it bounds the edit relative to the local neighborhood's natural scale on each axis, not to a global step size.

\paragraph{Why $\varepsilon$ transfers across datasets without per-panel tuning.}
A direct consequence of the whitening is that $\varepsilon$ no longer carries the units of the local representation geometry: each tangent axis is rescaled by its own $\sigma_{i,k}$ before $\varepsilon$ is applied, so a representation panel with a wide local spread along its dominant axes and one with a narrow local spread both report the same allowed move when expressed as a fraction of their respective whitened neighborhoods. This is why we can fix $\varepsilon = 0.1$ once and use it across all 39 NLP panels (13 language models $\times$ 3 concepts at 50\%-depth) and all 80 CelebA panels (40 attributes $\times$ 2 control sets) without any per-panel tuning of the trust-region radius. Crucially, this transferability requires that the per-axis scale $\sigma_{i,k}$ \emph{itself} be estimated from the local representation — exactly what the local SVD on the neighborhood-secant matrix provides. The legacy global $\lambda$ in §\ref{sec:evaluation} cannot have this property because its units are panel-specific: the same $\lambda$ moves a Gemma-3-27B representation by a much larger ambient distance than it does a Qwen2.5-0.5B one, which is why the legacy protocol must run a per-panel $\lambda$-HPO grid and ours does not.

\section{CelebA control-set construction (least- vs.\ most-correlated five attributes)}
\label{sec:appendix_celeba_controls}

For each of the 40 binary CelebA attributes used as a target concept, we report two complementary control regimes (Section~\ref{sec:celeba}, Table~\ref{tab:celeba_per_panel_breakdown}, Figure~\ref{fig:per_attr_bars_celeba}): the \emph{5 least-correlated} attributes and the \emph{5 most-correlated} attributes, both selected from the remaining 39 attributes. This appendix records exactly how the two control sets are computed.

\paragraph{Computation.}
Let $\mathbf{Y} \in \{0, 1\}^{N_{\text{train}} \times 40}$ be the train-split label matrix, with $\mathbf{Y}_{i,k}$ the label of training example $i$ on attribute $k$. We compute the empirical Pearson correlation matrix
\begin{equation}
\mathbf{R}_{k\ell} \;=\; \frac{\mathrm{Cov}(\mathbf{Y}_{:,k}, \mathbf{Y}_{:,\ell})}{\sqrt{\mathrm{Var}(\mathbf{Y}_{:,k}) \cdot \mathrm{Var}(\mathbf{Y}_{:,\ell})}}
\quad \text{for } k, \ell \in \{1, \ldots, 40\}.
\end{equation}
For each target attribute $k$, we then rank the remaining $39$ attributes by absolute correlation $|\mathbf{R}_{k\ell}|$:
\begin{itemize}
\item the \emph{5 least-correlated} controls are the bottom 5 by $|\mathbf{R}_{k\ell}|$ — they share \emph{minimal} information with the target and isolate clean surgicality;
\item the \emph{5 most-correlated} controls are the top 5 by $|\mathbf{R}_{k\ell}|$ — they share \emph{substantial} information with the target and form a stricter test of whether erasure can preserve genuinely related downstream features.
\end{itemize}
The target attribute itself is excluded from the candidate pool (i.e., $\ell \neq k$). Pairs are computed in float64 with no shrinkage. The full $40 \times 40$ correlation matrix and the resulting per-target control sets are stored in \texttt{outputs/celeba\_correlated\_controls.json} (committed for reproducibility) by \texttt{scripts/celeba\_compute\_correlations.py}.

\paragraph{Concrete examples.}
For \emph{Male}, the 5 most-correlated attributes are \emph{Wearing\_Lipstick} ($|r| \approx 0.78$), \emph{No\_Beard}, \emph{Heavy\_Makeup}, \emph{Attractive}, and \emph{5\_o\_Clock\_Shadow}; the 5 least-correlated are \emph{Narrow\_Eyes}, \emph{Blurry}, \emph{Straight\_Hair}, \emph{Receding\_Hairline}, and \emph{Pale\_Skin} (all $|r| < 0.05$). Erasing \emph{Male} while preserving the most-correlated controls is genuinely hard because the controls are partial proxies for the target; erasing while preserving the least-correlated controls tests whether the intervention is geographically narrow but not whether it disentangles correlated information. We therefore report both regimes side by side: each one isolates a different failure mode.

\paragraph{Why two regimes per concept.}
Reporting only one control set hides whichever regime the method happens to be weak in. A method that destroys the entire representation will pass the least-correlated test (controls are essentially independent of the edit anyway) and fail the most-correlated test catastrophically; conversely, a method that performs no erasure will pass both. The difference between the two regimes per method is therefore informative even when both individual numbers look acceptable. Section~\ref{sec:celeba} reads the difference as a measure of how much an erasure method confuses ``avoid this concept'' with ``avoid everything correlated with it''.

\section{Closed-form per-row $\lambda_i$ statistics}
\label{sec:appendix_lambda_stats}

For each panel and each outer iteration of the proposed pipeline (Phase 2 with fixed trust-region $\varepsilon = 0.1$), the closed-form rule of Eq.~\ref{eq:lambda_closed_form} produces a per-row step magnitude $\lambda_i$ (capped at $\lambda_{\max} = 64$). Table~\ref{tab:lambda_stats} reports the distribution of these per-row $\lambda_i$ averaged across all 60 iterations of each trajectory and then aggregated across panels.

\begin{table}[t]
\centering
\small
\caption{\textbf{Per-row $\lambda_i$ statistics for the proposed pipeline ($\varepsilon = 0.1$, $\lambda_{\max}=64$) on the 39-panel NLP grid at 50\%-depth.} Each cell averages the per-row $\lambda_i$ first across all 60 outer iterations of one trajectory, then across the 13 model panels of the concept. The within-panel $p_{10}/p_{90}$ spread (mean across panels) is roughly $14/45$, i.e.\ a $\sim$10$\times$ ratio between the least- and most-confident rows in a single iteration. The safety cap $\lambda_{\max}=64$ is observed only sporadically (max 57.32 across 39$\times$60 = 2340 iterations); the closed-form rule rarely binds against it.}
\label{tab:lambda_stats}
\begin{tabular}{l r r r r r}
\toprule
Concept & $n$ & mean $\lambda_i$ & median & min & max \\
\midrule
Sycophancy & 13 & 29.58 & 37.15 & 4.89 & 49.38 \\
Gender & 13 & 25.28 & 23.76 & 0.45 & 45.08 \\
Safety & 13 & 33.07 & 33.30 & 1.71 & 57.32 \\
\midrule
\textbf{Overall} & \textbf{39} & \textbf{29.31} & \textbf{30.19} & \textbf{0.45} & \textbf{57.32} \\
\bottomrule
\end{tabular}
\end{table}

The mean across the entire grid, $\bar{\lambda} \approx 29.31$, sits well above the legacy global-$\lambda$ HPO grid's typical winners ($\lambda^* \in \{1, 2, 4\}$ on most panels for the canonical AmbGCE/TanGCE form). This justifies the apples-to-apples ablation reported in Table~\ref{tab:multi_model}: when AmbGCE is forced to use a single global $\lambda = \bar{\lambda}_{\text{TanGCE}} = 29.31$ — i.e., the same effective step magnitude TanGCE's per-row rule produces — its iterative behaviour and final leakage are directly comparable, with the only difference being the gradient-direction constraint (tangent-projected vs. ambient).

\section{Anisotropy regimes for the local-$\sigma$ spectrum}
\label{sec:appendix_alpha_regimes}

Eq.~\ref{eq:tangent_erase_sigma} reweights each tangent coordinate $a_{i,k}$ by $\sigma_{i,k}^{\alpha}$, where $\alpha \in \mathbb{R}$ is an anisotropy exponent and $\sigma_{i,k}$ are the singular values of the local secant matrix $\mathbf{S}_i$ (Eq.~\ref{eq:secant_svd}). Three non-negative values of $\alpha$ have natural interpretations:
\begin{itemize}
\item \textbf{$\alpha = 0$}: the update is isotropic in the tangent basis and reduces to Eq.~\ref{eq:tangent_erase}. Reported as an ablation.
\item \textbf{$\alpha = 1$}: each tangent direction is reweighted by $\sigma_{i,k}$. \textbf{This is the default in this paper.} Equivalently, the update applies the positive square root of the local secant covariance, $\mathbf{C}_i^{1/2}$, since the eigenvalues of $\mathbf{C}_i = \tfrac{1}{k} \mathbf{S}_i^\top \mathbf{S}_i$ are proportional to $\sigma_{i,k}^2$.
\item \textbf{$\alpha = 2$}: each tangent direction is reweighted by $\sigma_{i,k}^2$, equivalent to applying $\mathbf{C}_i$. This is the natural-gradient / full-Mahalanobis trust-region optimum: it is the first-order solution of $\min_{\boldsymbol\delta} \langle \mathbf{g}_i, \boldsymbol\delta \rangle$ subject to $\boldsymbol\delta^\top \mathbf{C}_i^{-1} \boldsymbol\delta \le \varepsilon^2$. Reported as an ablation; risks suppressing low-variance signal that may carry residual concept information.
\end{itemize}

\paragraph{Relation to Mahalanobis whitening (and what $\alpha = 1$ is \emph{not}).}
A reader familiar with Mahalanobis whitening might wonder whether our $\alpha = 1$ is the same operation. It is not. Mahalanobis whitening transforms the data so the covariance becomes identity, $\mathbf{x}^{\mathrm{white}} = \mathbf{C}^{-1/2} \mathbf{x}$, and corresponds to scaling each tangent direction by $\sigma_{i,k}^{-1}$ — i.e.\ $\alpha = -1$ in our notation. Whitening makes every direction equal-weighted, which would dilute the concept-specific signal we want to suppress and force every tangent axis to receive equal step magnitude.

We use a \emph{positive} power, $\sigma_{i,k}^{+1}$, which does the opposite: it reweights the tangent step \emph{toward} high-$\sigma$ principal axes — exactly the directions where the local manifold has the most support and where the linear approximation is most reliable — and away from low-$\sigma$ thin tail directions where the linear approximation is fragile and motion most easily leaves the manifold. The three non-negative values $\alpha \in \{0, 1, 2\}$ form a one-parameter family interpolating between (i) treating every tangent direction as equally trustworthy ($\alpha = 0$, isotropic), (ii) reweighting by $\sigma_{i,k}$ to respect the local ellipsoid mildly ($\alpha = 1$, our default), and (iii) the natural-gradient / Mahalanobis trust-region optimum that maximally suppresses thin directions ($\alpha = 2$). Whitening ($\alpha = -1$) sits outside this range for our problem because its sign is opposite the geometric correction we want; we therefore do not include it.

\section{Coordinate-chart definition of the tangent space}
\label{sec:appendix_geometry}

We adopt the formal definition from \citet{tu-2011-manifolds}. Fix $\mathbf{x} \in \mathcal{M}$. A small local neighborhood $U \subset \mathcal{M}$ around $\mathbf{x}$ admits a coordinate map $\varphi : U \to \Omega \subset \mathbb{R}^m$ with $\varphi(\mathbf{x}) = 0$. The inverse $\varphi^{-1} : \Omega \to U \subset \mathbb{R}^d$ is a local parametrization back into the ambient hidden space. The tangent space at $\mathbf{x}$ is the $m$-dimensional linear subspace
\begin{equation}
T_{\mathbf{x}}\mathcal{M} = d(\varphi^{-1})_{0}(\mathbb{R}^m) \subset \mathbb{R}^d,
\end{equation}
where $d(\varphi^{-1})_0$ is the differential of $\varphi^{-1}$ at the origin (its Jacobian, in ML language). It maps an infinitesimal coordinate displacement in $\mathbb{R}^m$ to the corresponding first-order ambient displacement. Because $\mathcal{M}$ is embedded in $\mathbb{R}^d$, $T_{\mathbf{x}}\mathcal{M}$ is an $m$-dimensional linear subspace of the ambient hidden space.

In \TanGCE{} we never construct $\varphi$ or $\varphi^{-1}$. The leading right singular vectors of the local secant matrix (Eq.~\ref{eq:secant_svd}) serve as an empirical first-order basis for $T_{\mathbf{x}}\mathcal{M}$, and the orthogonal projector $\mathbf{P}_{T_i} = \mathbf{B}_i \mathbf{B}_i^\top$ implements the tangent component $\mathbf{g}^{\top}$ of any ambient vector $\mathbf{g}$. We do not claim that $\mathcal{M}$ is smooth in any theorem-level sense, only that this approximation tracks the local sample structure well enough to attenuate updates in directions unsupported by it.

