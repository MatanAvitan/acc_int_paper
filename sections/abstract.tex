% Abstract
\section*{Abstract}

Concept erasure should remove a target concept without damaging the rest of the representation, but nonlinear concepts remain recoverable after standard linear interventions and unconstrained ambient-space edits can distort representations \ye{the focus shouldn't compare to linear methods}\ye{unclear what 'ambient-space' is} \ma{we will describe in-depth what an ambient space is, i dont see why it's a problem to include this vivid term in the abstract}\ye{what do you want readers to take away from your abstract? i didn't say you shouldn't include it, but you should briefly explain important terms you're using here to give an idea.}. We propose \TanGCE{}, a new manifold-aware erasure method that estimates local tangent spaces with neighborhood PCA and restricts erasure updates to that local support \ye{this sentence has a lot of jargon. tone it down} \ma{There's no argument to tone town here, it's simply a factual description of the method.}\ye{it's not simple as it's not clear what you do...}. The core idea is simple \ye{<- LLMi. instead, something like: `we rely on the intuition that interventions should follow the same manifold where natural representations lie'} \ma{I dont see the problem with the current phrasing}\ye{it sounds like it's coming out from a blog post, not a scientific paper}: if natural representations lie on a low-dimensional manifold inside the ambient space, then effective interventions should follow that manifold rather than push representations away from it. Across our completed \ye{huh? give a number/extensive experiments} text and vision benchmarks, \TanGCE{} delivers the strongest overall nonlinear erasure results in the study: it consistently beats the prior learned baselines \ye{instead -> achieves state of the art results / surpasses all baselines}, including \AmbGCE{}, and avoids the severe control damage incurred by distortion-based baselines. 